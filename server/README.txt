requires npm (Node package manager) installed
http://npmjs.org/

# Installation
# ------------

git clone git://github.com/isaacs/npm.git

# Choose a stable tag to install
# ------------
git checkout v1.0.1rcFINAL
sudo make install

# And socket io installed with npm
npm install socket.io
